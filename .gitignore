# PATH: .gitignore

# --- REPLACE START ------------------------------------------------------------
# -----------------------------------------------------------------------------
# Node modules
# -----------------------------------------------------------------------------
node_modules/
client/node_modules/
server/node_modules/

# -----------------------------------------------------------------------------
# Build artifacts
# -----------------------------------------------------------------------------
dist/
client/dist/
server/client-dist/
client-dist/

# -----------------------------------------------------------------------------
# User uploads (never in git)
# -----------------------------------------------------------------------------
uploads/
server/uploads/

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
*.log
**/*.log

# -----------------------------------------------------------------------------
# Environment files (real secrets) — keep only examples in git
# -----------------------------------------------------------------------------
# Do NOT commit any real env files
.env
.env.*
*.env
*.env.*.local
server/.env
client/.env
client/.env.local

# Keep example templates tracked
!.env.example
!server/.env.example
!client/.env.example

# -----------------------------------------------------------------------------
# Diagnostics / temporary bundles
# -----------------------------------------------------------------------------
_diag/

# -----------------------------------------------------------------------------
# Editor & OS temp files
# -----------------------------------------------------------------------------
.vscode/
.idea/
.DS_Store

# -----------------------------------------------------------------------------
# Coverage reports
# -----------------------------------------------------------------------------
coverage/

# -----------------------------------------------------------------------------
# i18n cleanup backups and generic .bak files
# -----------------------------------------------------------------------------
client/public/locales/**/*.bak
*.bak
# --- REPLACE END --------------------------------------------------------------
_trash/

# Windows device ghost
NUL
nul

# Diagnostic/pack bundles (keep out of git)
_diag/
_pack/


# local generated / backup / reports
artifacts/
lighthouse_prod_*/
preview_out_*.txt
preview_err_*.txt
client/public/_asset_backup_*/
client/public/_hero_backup_*/

# openapi phase packs (break jest haste map)
_openapi_phase_pack/
server/__openapi_phase_pack/


# OpenAPI phase pack (temporary)
_openapi_phase_pack/
server/__openapi_phase_pack/
_trash_openapi/