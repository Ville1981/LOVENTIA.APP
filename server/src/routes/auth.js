// File: server/src/routes/auth.js
// Shim router: delegates to ../routes/auth.js
// Keeps compatibility whether app.js looks in src/routes or routes

import router from "../../routes/auth.js";
export default router;
