# --- REPLACE START: environment configuration ---

###############################################
# MongoDB (local development)
# Use IPv4 loopback to avoid IPv6 resolver issues on Windows
###############################################
MONGO_URI=mongodb://127.0.0.1:27017/loventia

###############################################
# MongoDB (Docker Compose)
# docker-compose will construct MONGO_URI using these.
# NOTE: When running in Docker, the server service uses host "mongo"
# and overrides MONGO_URI via docker-compose.yml (environment section).
###############################################
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=changeme
MONGO_DB_NAME=loventia
MONGO_PORT=27017

###############################################
# App runtime
###############################################
NODE_ENV=development
PORT=5000

# If your API is mounted under /api, keep the suffix
BASE_URL=http://localhost:5000/api

###############################################
# CORS / Frontend
###############################################
# Vite dev server (local)
CLIENT_URL=http://localhost:5174

###############################################
# JWT authentication
###############################################
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here

###############################################
# Stripe (test keys/placeholders)
###############################################
STRIPE_SECRET_KEY=sk_test_yourkey
STRIPE_WEBHOOK_SECRET=whsec_yoursecret
STRIPE_PREMIUM_PRICE_ID=price_yourid

###############################################
# PayPal (sandbox placeholders)
###############################################
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_SECRET=your_paypal_secret
PAYPAL_PREMIUM_PRICE=12.00
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id

###############################################
# SMTP (example: MailDev on localhost:1025)
###############################################
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM_NAME=LoventiaApp
# Optional: MAIL_FROM_EMAIL=no-reply@loventia.local

###############################################
# Test user for smoke tests
###############################################
TEST_EMAIL=tester+auth@loventia.local
TEST_PASSWORD=VeryStrong!123

# --- REPLACE END ---

