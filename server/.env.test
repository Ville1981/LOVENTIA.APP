# /server/.env.test

# Use with MongoMemoryServer; dummy URI suffice
MONGO_URI=test

# Application environment for tests
NODE_ENV=test
PORT=5001

# JWT secrets for signing test tokens
JWT_SECRET=testsecret
JWT_REFRESH_SECRET=testrefreshsecret

# Stripe & PayPal test keys (no real network calls)
STRIPE_SECRET_KEY=sk_test_dummy
STRIPE_WEBHOOK_SECRET=whsec_test
STRIPE_PREMIUM_PRICE_ID=price_test

PAYPAL_CLIENT_ID=paypal_test_client
PAYPAL_SECRET=paypal_test_secret
PAYPAL_PREMIUM_PRICE=0.00
PAYPAL_WEBHOOK_ID=wh_test

# SMTP (MailDev) dummy settings
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM_NAME="TestApp"

# Disable notifications in test environment
SLACK_WEBHOOK_URL=
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
ALERT_EMAIL_FROM="Test Alerts" <no-reply@example.com>
ALERT_EMAIL_TO=""

# Thresholds relaxed for tests
ERROR_RATE_THRESHOLD=100
LATENCY_THRESHOLD_MS=1000
THROUGHPUT_THRESHOLD_RPM=100
