# MongoDB credentials (for Docker-based development)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=secret

# Primary MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/LoventiaApp

# Application environment
NODE_ENV=development
PORT=5000

# JWT secrets
JWT_SECRET=salainenavain123
JWT_REFRESH_SECRET=supersecretrefreshkey987

# Stripe payment settings
STRIPE_SECRET_KEY=sk_test_51RM2522fH99StxzonioarhVVsUZBqN7GKgMH0m5qvfGsP2Wyzzrfm0Cd4Peb02TLUEpHtQblaI0BO3FANYgu8KXB00xL44x45L
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
STRIPE_PREMIUM_PRICE_ID=price_...

# PayPal payment settings
PAYPAL_CLIENT_ID=AbCdEfGhIjKlMnOpQrStUvWxYz123456
PAYPAL_SECRET=EFGHijklMNOPqrstUVwxYZ7890abcd
PAYPAL_PREMIUM_PRICE=12.00
PAYPAL_WEBHOOK_ID=<your-paypal-webhook-id>

# SMTP configuration for local development (MailDev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM_NAME="MyApp"

# CORS origin and password-reset link base URL
CLIENT_URL=http://localhost:5174

# --- REPLACE START: Notification integrations ---
# Slack webhook for alertRules.js
SLACK_WEBHOOK_URL=

# Nodemailer / Email alerts
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
ALERT_EMAIL_FROM="MyApp Alerts" no-reply@example.com
ALERT_EMAIL_TO="team@example.com,ops@example.com"
# --- REPLACE END ---

# --- REPLACE START: Threshold configurations ---
# Critical metric thresholds (for checkThreshold in alertRules.js)
ERROR_RATE_THRESHOLD=50           # in percent (test)
LATENCY_THRESHOLD_MS=100          # in milliseconds (test)
THROUGHPUT_THRESHOLD_RPM=1        # in requests per minute (test)
# --- REPLACE END ---

# Serve /locales via Express (optional). Client can serve locales itself.
USE_SERVER_LOCALES=false
