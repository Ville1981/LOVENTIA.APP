# PATH: server/openapi.yaml
# --- REPLACE START
openapi: 3.1.0

info:
  title: Loventia API (shim)
  version: 1.0.0
  description: |
    This is a legacy shim OpenAPI document kept for backwards compatibility.
    The real, up-to-date Loventia API specification lives at:

      server/openapi/openapi.yaml

    All tooling (Spectral, Redocly, CI workflows, local preview) should
    use the main spec:

      ./openapi/openapi.yaml

    This file is intentionally minimal and is not used by the current
    OpenAPI CI pipeline.

servers:
  - url: http://localhost:5000/api
    description: Local backend API base (shim reference)

externalDocs:
  description: Main Loventia OpenAPI document
  url: ./openapi/openapi.yaml
# --- REPLACE END






