// File: client/performance/socket-reconnect.test.js

// --- REPLACE START ---
// Vitest stub: the real socket reconnect load test should be run with k6,
// not Vitest. Execute with:  k6 run client/performance/socket-reconnect.k6.js
describe("Socket reconnect load test (k6)", () => {
  it("should be executed with k6, not Vitest", () => {
    expect(true).toBe(true);
  });
});
// --- REPLACE END ---
