# File: client/.env.local

VITE_BACKEND_URL=http://localhost:5000
VITE_CLIENT_URL=http://localhost:5174

# --- REPLACE START: Development API base URL (absolute to avoid URL() errors) ---
# In local dev we make API base absolute so any `new URL(API_BASE_URL)` calls are valid.
# Axios + our config helpers will still work correctly with this.
VITE_API_BASE_URL=http://localhost:5000/api
# --- REPLACE END ---

# Optional: explicit backend origin for building absolute asset URLs (uploads, etc.)
VITE_BACKEND_ORIGIN=http://localhost:5000

# Toggle Mock Service Worker in development
VITE_ENABLE_MSW=false

# Public placeholder image path (served from /public)
VITE_PLACEHOLDER_IMAGE=/placeholder-avatar-male.png

# --- REPLACE START: Ads feature flags (local dev defaults) ---
# Centralized ad flags used by useAds/AdGate; adjust as needed during development.
VITE_ADS_ENABLED=true
VITE_ADS_INTERSTITIAL_ENABLED=true
VITE_ADS_OVERLAY_ENABLED=true
VITE_ADS_INLINE_ENABLED=true
# CAP DISABLED for unlimited inline/interstitial impressions during dev verification
VITE_ADS_FREQ_CAP_PER_DAY=0
# Optional: header image source referenced by MainLayout header ad
VITE_HEADER_AD_SRC=/ads/header1.png
# Interstitial creative (served from /public)
VITE_INTERSTITIAL_AD_SRC=/ads/interstitial1.png
# --- REPLACE END ---


