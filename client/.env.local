VITE_BACKEND_URL=http://localhost:5000
VITE_CLIENT_URL=http://localhost:5174

# --- REPLACE START: Production API base URL
# Use only in production or deployed environment, not in local dev.
# For local dev, comment this out so that /api (Vite proxy) is used instead.
# VITE_API_BASE_URL=https://loventia.app/api
# --- REPLACE END ---

VITE_ENABLE_MSW=false
