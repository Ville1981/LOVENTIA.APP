# --- REPLACE START: ensure Vite proxies API calls under /api and define backend origin used by the UI ---
# Base path for API requests from the frontend. Keep as /api when the dev server proxies to backend.
VITE_API_BASE_URL=http://localhost:5000

# Explicit backend origin for building absolute asset URLs (e.g., /uploads/**).
# Used by Photos.jsx and other helpers.
# In development, your backend usually runs at http://localhost:5000
VITE_BACKEND_ORIGIN=http://localhost:5000

# Toggle Mock Service Worker in development (false by default)
VITE_ENABLE_MSW=false
# --- REPLACE END ---

# Any other VITE_* vars can go here
# Example backend URL for direct calls (kept for backward compatibility if some modules use it)
VITE_BACKEND_URL=http://localhost:5000

# Frontend dev server URL (used by CORS allowlists and redirects)
VITE_CLIENT_URL=http://localhost:5174

# Placeholder image path (served from public/)
VITE_PLACEHOLDER_IMAGE=/placeholder-avatar-male.png














