# File: .github/zap/zap-rules.tsv

# --- REPLACE START: Zap rules (downgrade some infos to warn) ---
# id  threshold   action
10020   WARN    ignore  # X-Frame-Options header not set (use CSP frame-ancestors in prod)
10038   WARN    ignore  # Content Security Policy (if not enforcing yet)
10021   WARN    ignore  # X-Content-Type-Options missing (helmet should add; warn until enforced)
# --- REPLACE END ---
10054	IGNORE	# Cookie without HttpOnly flag (consent libs)
10055	IGNORE	# X-Content-Type-Options header missing (serve via CloudFront headers)
10021	IGNORE	# X-Frame-Options header missing (handled by ALB/CF/WAF)
